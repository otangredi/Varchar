<div class="container pt-5" style="width: 900px;">
  <div class="border rounded p-5">
  <h1 class="mb-4"><strong>Your booking details</strong></h1>

  <div class="d-flex mb-4">
    <%= cl_image_tag @booking.character.photos.all.first.key, :gravity=>"face", :height=>100, :width=>100, :crop=>"thumb", class: "rounded-4" %>
    <div>
      <h4 class="px-4 pt-3"><%= @booking.character.name %></h4>
      <p class="px-4"><%= @booking.character.address %></p>
    </div>
  </div>

  <h4><strong>Dates</strong></h4>
    <p class="mb-3"><%= @booking.start_date.strftime("%b.%d") %> - <%= @booking.end_date.strftime("%d") %></p>
  <div class="border-bottom"></div>
    <h4 class="mt-3"><strong>Price details</strong></h4>
  <div class="d-flex justify-content-between">
    <p>$ <%= @booking.character.price %> x <%= (@booking.end_date. - @booking.start_date + 1).to_i %> day</p>
    <p>$ <%= @booking.character.price.to_i * (@booking.end_date. - @booking.start_date + 1).to_i %></p>
  </div>
  <div class="d-flex justify-content-between">
    <p>Delivery fee</p>
    <p>$ 10</p>
  </div>
  <div class="border-bottom mb-3"></div>
    <div class="d-flex justify-content-between">
      <p><strong>Total(CAD)</strong></p>
      <p>$ <%= @booking.character.price.to_i * (@booking.end_date. - @booking.start_date + 1).to_i + 10 %></p>
    </div>
  <div class="border-bottom"></div>
  <p class="mt-2"><small>Your character may charge an additional fee for emotional damage.</small></p>
  <%= link_to 'Cancel', delete_booking_path(@booking), class: "btn btn-primary w-100 mt-4", data: { turbo_method: :delete, turbo_confirm: 'Are you sure you want to cancel?' }%>
  </div>
</div>
